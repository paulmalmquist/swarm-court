# Publication status

**Repository:** `paulmalmquist/swarm-court`  
**Visibility:** Public  
**Default branch:** `main`

The synthetic-only Swarm Court seed has been published. The public repository intentionally excludes credentials, runtime databases, local caches, proprietary work data, and unrelated attachments.

Clone and run:

```bash
git clone https://github.com/paulmalmquist/swarm-court.git
cd swarm-court
python -m src.server
```

The public source keeps Claude execution disabled and all work-system connections unconfigured. Work integration starts from `WORK_HANDOFF.md`.
